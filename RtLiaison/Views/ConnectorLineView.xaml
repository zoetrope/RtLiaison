<UserControl x:Class="RtLiaison.Views.ConnectorLineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:spline="clr-namespace:CanonicalSplineLib" xmlns:l="clr-namespace:RtLiaison.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <l:PointCollectionConverter x:Key="pointCollectionConverter"/>
    </UserControl.Resources>
    <Grid Name="grid">
        <!-- 線分情報をbindingする -->
        <!-- http://social.msdn.microsoft.com/Forums/en-US/wpf/thread/1293d4cb-87b3-4cdc-97e3-ae2f41caf2d4/ -->

        <!-- PolyQuadraticBezierSegment(ベジェ)では指定した点を通らないのでスプライン -->
        <spline:CanonicalSpline Stroke="Red" StrokeThickness="3" Tension="1"
                                Points="{Binding Points, Converter={StaticResource pointCollectionConverter}}"/>
    </Grid>
</UserControl>
